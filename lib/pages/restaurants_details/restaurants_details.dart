import 'package:flutter/material.dart';

class RestaurantsDetails extends StatelessWidget {
  const RestaurantsDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
